spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jobsitedb?useSSL=false&useUnicode=yes&characterEncoding=UTF-8
    username: root
    password: password
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
server:
  port: 9009
